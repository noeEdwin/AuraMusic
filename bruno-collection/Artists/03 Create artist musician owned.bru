meta {
  name: Create artist musician owned
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/artists
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{musicianToken}}
  Content-Type: application/json
}

body:json {
  {
    "name": "Bruno Musician Artist",
    "bio": "Artista propio creado por MUSICIAN"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setVar("artistId", data.id);
}
