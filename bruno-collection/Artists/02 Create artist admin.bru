meta {
  name: Create artist admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/artists
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  {
    "name": "Bruno Collection Artist",
    "bio": "Artista creado durante la prueba de Bruno"
  }
}
