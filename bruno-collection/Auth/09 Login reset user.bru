meta {
  name: Login reset user
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{resetEmail}}",
    "password": "{{resetNewPassword}}"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setVar("token", data.token);
}
